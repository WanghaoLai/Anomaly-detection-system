# Python >= 3.11（开发环境实测 3.13）
# 所有直接依赖统一使用 == 精确锁定，保证迁移安装可复现

# 核心框架
fastapi==0.115.14
uvicorn==0.44.0
python-multipart==0.0.29
python-dotenv==1.2.3
pydantic==2.13.4

# 数据库（Tortoise ORM + 异步 MySQL 驱动；PyMySQL 由 aiomysql 传递引入）
tortoise-orm==0.25.4
aiomysql==0.3.2

# 认证与安全（JWT 用 python-jose；密码哈希直接用 bcrypt）
python-jose==3.5.0
bcrypt==5.0.0

# AI / LLM 与向量数据库（requests、aiohttp 由 dashscope 传递引入）
dashscope==1.25.16
chromadb==1.5.9
llama-index-core==0.14.23
llama-index-vector-stores-chroma==0.5.5

# 文档解析（RAG 知识库入库）
# pypdf/pdfminer/pdfplumber/python-docx/python-pptx/mammoth/openpyxl/beautifulsoup4
# 均由下列 extras 统一解析版本，无需重复声明
markitdown[docx,pdf,pptx,xlsx,xls]==0.1.6

# 图像校验（上传文件服务端解码验证）
pillow==12.3.0

# 远程 GPU 服务器 SSH（训练/推理执行器）
asyncssh==2.22.0

# NLP (可选，按需启用)
# spacy==3.8.14
# sentence-transformers==5.5.1
# transformers==5.9.0
# torch==2.12.0

# Agent 框架 (可选)
# autogen-agentchat==0.7.5
# mcp==1.27.1
