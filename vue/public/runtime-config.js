// Deployments may replace this file without rebuilding the frontend.
// Keep apiBaseUrl empty to use the same-origin /api default.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {
  apiBaseUrl: '',
}
